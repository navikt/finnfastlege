apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: fastlegefront
  namespace: default
  labels:
    team: teamsykefravr
spec:
  image: "docker.pkg.github.com/navikt/fastlegefront:2019.11.01-90d1410"
  replicas:
    min: 1
    max: 2
    cpuThresholdPercentage: 90
  port: 8080
  liveness:
    path: /health/isAlive
    initialDelay: 10
    timeout: 5
    periodSeconds: 5
    failureThreshold: 30
  readiness:
    path: /health/isReady
    initialDelay: 30
    periodSeconds: 5
    timeout: 5
  # prometheus:
  #   enabled: false
  #   path: /actuator/metrics
  vault:
    enabled: true
  env:
    - name: ENV
      value: q1
    - name: HOST
      value: app-q1.adeo.no
  resources:
    limits:
      cpu: 500m
      memory: 1Gi
    requests:
      cpu: 100m
      memory: 128Mi
  webproxy: false
  ingresses:
    - "https://fastlegefront.nais.preprod.local"
    - "https://app-q1.adeo.no"
